# This is a basic workflow to help you get started with Actions

name: checking review system
on:
  push:
    branches: main
jobs:
  DEV:
    runs-on: ubuntu-latest
    environment: Dev
    steps:
      - name: Checkout
        uses: actions/checkout@v3.5.2
        
      - name: Bump version and push tag
        uses: hennejg/github-tag-action@v4.1.jh1
        with:
          github_token: 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
        
        
       # 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
        
      #- name: Semantic versioning
      #  uses: paulhatch/semantic-version@v5.0.2
       # with:
        #  branch: main
         # tag_prefix: "v"
          ##inor_changes: "feat:"
          f#ormat: "v${major}.${minor}.${patch}-prerelease${increment}"
          
      - name: say something
        run: echo "hello world, This is Dev Environment - lets check now"
  
  QA:
    runs-on: ubuntu-latest
    environment: QA
    needs: DEV
    steps:
      - name: Checkout
        uses: actions/checkout@v3.5.2
          
      - name: say something
        run: echo "hello This is QA Environment"
      
  Pre-pod:
    runs-on: ubuntu-latest
    environment: Pre-pod
    needs: QA
    steps:
      - name: Checkout
        uses: actions/checkout@v3.5.2
          
      - name: say something
        run: echo "hello world, This is Pre-pod Environment"
     
     
     
          



